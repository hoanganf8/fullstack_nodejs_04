GET /users/?id=1&key=abc
--> header --> Giới hạn
--> body --> Không có ở GET

# Transformer

HTTP Request ==> Global Middleware ==> Route ==> Route Middleware ==> Controller ==> Transformer ==> HTTP Response (JSON)
